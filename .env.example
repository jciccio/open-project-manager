# Copy to .env (or .env.local for `next dev`) and fill in real values.
# Generate a secret with: openssl rand -base64 32
JWT_SECRET=

# Optional — defaults to file:./dev.db if unset.
DATABASE_URL=file:./dev.db
